#Before executing(running the files in jenkins), place an order in msite and make sure all the functionalities are working and 
there by copy the id of the placed order from the orders table of DB. And paste it in collection variables.
#Testing the changes
